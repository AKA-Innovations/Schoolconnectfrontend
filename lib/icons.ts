import { LucideIcon, School, Users, GraduationCap, CreditCard, CheckCircle, Clock, BookOpen, FileText, AlertTriangle, Layers, ClipboardList, BarChart } from 'lucide-react';

export const IconMap: Record<string, LucideIcon> = {
  School,
  Users,
  GraduationCap,
  CreditCard,
  CheckCircle,
  Clock,
  BookOpen,
  FileText,
  AlertTriangle,
  Layers,
  ClipboardList,
  BarChart,
};
